# セキュリティ改善・再監査（2026-09-19）

## 結果

前回再現した3件を修正し、ローカル環境で回帰検証した。今回確認した経路では、修正対象の再現手順による不正操作は成立しない。本番への公開・本番データへの検査は実施していない。

| 対象 | 以前の問題 | 修正・検証結果 |
| --- | --- | --- |
| 連合投稿の所有権（高） | 正しいインスタンス署名があれば、別の投稿者の保存済み投稿を更新・削除できた | 保存済みの投稿者を行ロック下で照合。別インスタンス・同一インスタンスの別投稿者とも403。本文・削除状態・投票・アカウント情報の不変を確認 |
| OAuthクライアント削除（中） | 削除済みクライアントのアクセストークンが有効期限まで使えた | 毎回クライアントの存在を確認し、削除済み・不正ID・確認失敗を拒否。認可コード方式・クライアント認証方式とも削除後401 |
| レスポンスキャッシュ（中） | 不明なscopeごとに期限切れエントリーとロックが蓄積した | scopeを検証。各キャッシュ最大256件、LRU退避、参照・追加時の期限切れ削除、固定64本のロックに変更 |

## 実装と追加点検

- 投稿作成の衝突・更新・削除・投票集計を共通の所有権チェックに通す。本文、ハッシュタグ、投票、投稿者メタデータを同一トランザクションで保存し、失敗時は全体をロールバックする。通知はコミット成功後に送る。
- 不存在の投稿への更新・削除は無操作で成功する。削除済み投稿は再送によって復活しない。作成の同時衝突では一方だけが所有者になり、他方は403になる。
- 隣接する投票集計にも同じ欠陥があったため修正した。「いいね」集計の中継は保存済み投稿のインスタンスの署名を要求し、同一インスタンスの別投稿者による正規の中継を許可する。ローカル投稿への「いいね」処理は維持した。
- 所有権確認前のアカウント情報更新を投稿トランザクション内に移した。署名付きイベントで他人のportable IDを自己申告しても、その人の保存済みアカウント情報を上書きできない。
- 移行イベントは著者と旧アカウント・portable IDの対応を確認する。既存の署名元検証を維持し、検証済み移行に合わせて保存済み投稿者を更新する。移行の再送で旧所有者が移行先のメタデータを上書きしないことも検証した。
- OAuth認可コードの利用者とアプリ所有者が異なる場合を許可する。通常のログインセッション・PATはOAuthクライアントの存在確認に依存させない。
- `scope`未指定と`all`を同一キーに正規化する。`following`・`recommended`は維持し、不明値はページ位置にかかわらず400で拒否する。
- DBスキーマ・JWT形式は変更していない。依存パッケージの更新は不要だった。

## 実行した検証

| 検証 | 結果 |
| --- | --- |
| `go test -count=1 ./...`（下記の統合テスト環境変数を設定） | 全パッケージ成功 |
| 署名付き連合イベント統合テスト | v2/v3の別所有者拒否、投票・アカウントの保護、通知抑止、DB失敗時のロールバック、正規更新・削除、不存在・削除済み投稿、再送、同時処理、移行を確認 |
| キャッシュ・scope・OAuthのGo回帰テスト | 上限、LRU、期限切れ、同一キーの重複読込抑制、並行更新、読込失敗、失効・DB失敗時の拒否を確認 |
| `go test -race`（上記キャッシュ・scope・OAuthテスト） | 成功。Windows側はCGO無効のため、一時LinuxコンテナでCGOを有効にして実行 |
| `python scripts/security-smoke.py` | 両OAuthフローの削除前成功・削除後401、別利用者の認可、セッション・PAT・scope、登録・メール認証・画像の公開範囲・GET/HEAD/Range・パスワード保護・ログアウト失効が成功 |
| `npm run typecheck` | 成功 |
| `npm run test` | 9ファイル・34テスト成功 |
| Dockerでのバックエンド・フロントエンド本番ビルド | 成功 |
| `npm audit --json` | 既知の脆弱性0件 |
| `govulncheck -show verbose ./...` | 到達可能な脆弱性0件、インポート済みパッケージの指摘0件 |

Go依存モジュールには`GO-2026-5932`（保守されていない`golang.org/x/crypto/openpgp`）の情報があるが、Glipzは当該パッケージをインポート・呼び出ししていない。実行経路の脆弱性としては数えていない。

統合テストは`GLIPZ_SECURITY_TEST_DATABASE_URL`と`GLIPZ_SECURITY_TEST_REDIS_URL`を明示した場合だけ実行する。マイグレーション済みのローカル検証DBと専用Redis DBを使用する。外部ディスカバリー通信はテスト用トランスポートで置換し、署名自体は実際のEd25519鍵で生成・検証する。DBレコード・nonce・イベントID・メール・テストアカウントはテスト終了時に削除する。テスト内の意図的な投票制約違反による500ログは、ロールバック確認の期待結果である。

## 残存リスク・未検証範囲

- 修正対象の確認済み問題は解消したが、アプリケーション全体に脆弱性が存在しないことを保証する監査ではない。
- 本番のTLS・リバースプロキシ・権限・ストレージ設定、DM暗号方式、IDのエクスポート／インポート全体、実際の他インスタンスとの総合相互運用は網羅的に検証していない。
- 所有権はインスタンス署名と保存済み投稿者を信頼する。署名鍵を持つインスタンス自身による自ドメインのアカウント偽装を、今回のチェックだけで防ぐものではない。
- キャッシュは件数の上限であり、バイト数の上限ではない。アクセスのない期限切れエントリーは次の参照・追加まで最大256件の範囲で残る。固定ロックの衝突時は異なるキーの読込が直列化する。
- DB障害時はOAuth認証を拒否する。これは削除済みクライアントを誤って許可しないための意図した動作である。
- 既に発生した改ざんの本番データ調査・復旧は実施していない。
