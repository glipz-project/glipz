# セキュリティ改善プラン

作成日: 2026-09-19  
根拠: [セキュリティ監査](SECURITY_AUDIT.ja.md)（対象コミット `83932f9de710410eed693bd65c511bfe250feecf`）

目的は、内部ネットワークへの不正な通信、保護対象メディアへの無権限アクセス、ログアウト後の認証継続、通知送信による資源枯渇を防ぐこと。以下は当初の計画。2026-09-19にリポジトリ内の修正とローカルDocker環境への反映を実施した。実施内容・検証・残作業は[実施報告](SECURITY_IMPLEMENTATION.ja.md)を参照。本番環境は未定のため、本番変更は実施していない。

## 1. 優先順位と進め方

| 順序 | 作業 | 優先度 | 完了の判断 |
|---|---|---|---|
| 0 | 本番設定と露出経路の確認、必要な暫定制限 | 即時 | Push有効状態、ストレージ公開状態、ビルド版、プロキシ設定を把握 |
| 1 | PushのSSRFとタイムアウトを同時修正 | P1 | 内部宛先を遮断し、送信が必ず期限内に終了 |
| 2 | メディアの認可と既存データの配信経路移行 | P1 | 元URL・CDN・Rangeを含め、無権限の取得を拒否 |
| 3 | Goと依存関係を更新 | P1相当の更新作業 | 配布するビルドに対する再スキャンと互換性検証が成功 |
| 4 | セッション失効を実装 | P2 | ログアウト済みトークンを全APIインスタンスが拒否 |
| 5 | CIと運用監視で再発を防止 | P2 | 修正した問題を回帰テストで検出できる |

Pushの二つの指摘は同じ送信経路にあるため、一つの変更として扱う。依存関係更新は機能修正と別のPRにし、メディア改修を待たずに検証可能。各PRに以下の受入条件を記載する。

## 2. 本番の確認と暫定対応

本番環境は監査していないため、まず担当者が次を確認する。秘密鍵やトークンの値をログや報告書に記載する必要はない。

- Web Pushが有効か、どのブラウザー／Pushサービスをサポートしているか。
- メディアがローカル、S3、CDNのどの経路から取得できるか。公開バケットや独自ドメインからの直接取得も確認する。
- 実際に稼働しているバイナリのGo版、コンテナのベースイメージ、DB接続モード。
- `TrustProxyHeaders`の有効状態、バックエンドへの直接到達可否、プロキシによる転送ヘッダーの上書き。

Pushが有効なら、修正反映まで一時停止する案を優先する。ネットワーク側で送信先を制限する場合は、正規のDB・Redis等への通信を一括遮断しない。

保護対象メディアが公開経路に残っている場合は、該当経路の配信停止またはアクセス制限を行う。これは表示停止を伴うため、対象を洗い出してから適用する。公開済みのファイルのコピーや利用者端末のキャッシュは回収できないことを区別する。

## 3. PR 1: Web Pushの安全な送信

主な対象: `backend/internal/httpserver/webpush.go`、`outbound_http.go`、Push購読のDB処理、設定。

### 実装

1. Push専用のURL検証を用意する。HTTPSのみ許可し、ユーザー情報付きURL、不要なポート、ローカル名、非公開・予約アドレスを拒否する。許可するサービスのホストは対応ブラウザーに基づいて定義し、部分文字列の一致で許可しない。
2. 登録時だけでなく送信時にも検証する。既存の購読情報にも同じ制限を適用し、不正なendpointは送信せず無効化する。
3. DNS解決後、実際に接続するIPを検査してそのIPに接続する。再解決によるすり替えを防ぐ。Pushのリダイレクトは原則拒否する。環境変数由来のHTTPプロキシを暗黙に継承してIP検査を迂回しない設計にする。
4. `SendNotificationWithContext`に15秒の送信期限を渡し、HTTPClient側にも上限を設定する。レスポンスBodyを確実に閉じる。
5. goroutineを通知ごとに無制限に作る構造を、件数と並列数に上限のある送信キューに置き換える。購読件数と登録頻度にも上限を設ける。具体的な値は想定利用者数とステージング負荷試験から決定する。
6. 通知先の完全なURL、購読鍵、通知本文を監視ログに残さず、拒否理由・タイムアウト数・キュー長を集計する。

### 受入条件

- localhost、IPv4/IPv6の非公開宛先、IPv4-mapped IPv6、DNSの応答変更、内部宛先へのリダイレクトを拒否する。
- DBに既に登録された不正endpointも拒否する。
- 応答が停止したテストサーバーに対し、期限超過で終了し、同時接続とキュー長が設定上限を超えない。
- 正規のPushサービスによる通知が、対象ブラウザーで到達する。
- API登録→DB保存→通知発生→送信という経路を、実データを使わない統合テストで確認する。

## 4. PR 2: メディア配信時の認可

主な対象: `public_media.go`、投稿の閲覧権限判定、メディアと投稿のDB関連付け、`s3client`、フロントエンドのメディアURL生成。

### 採用方針

最初の修正は、保護対象メディアをバックエンド経由で配信し、リクエストごとに認証・認可する方式とする。短期署名URLは配信性能が必要になった場合の追加案とする。署名URLには有効期間中の再共有・失効遅延があるため、即時失効と同等には扱わない。

### 実装

1. アバター等の公開アセット、投稿の添付、未投稿のアップロード、暗号化DM添付を分類する。DM添付は既存の暗号化・参加者制限との整合性を別途確認する。
2. オブジェクトキーから所有者・用途・投稿を特定できる関連付けを用意する。URLやキーを知っていることだけでは認可しない。未関連付けのアップロードは所有者のみ許可する。
3. 投稿APIと配信APIで同じ閲覧判定を使用する。公開範囲、公開予約日時、削除・停止状態、パスワード解除、会員資格を反映する。DB等の障害時は保護対象を返さない。
4. 同一ファイルが公開投稿と非公開投稿に再利用される場合の意味を明確にする。公開用と保護用のオブジェクトまたは関連付けを分け、公開URLから保護コピーに到達できないようにする。
5. GET、HEAD、Rangeのすべてで認可する。`Sec-Fetch-Dest`やRefererは認可の根拠にしない。保護対象は`Cache-Control: private, no-store`とし、共有キャッシュに保存させない。
6. 保護対象のS3/CDN直接配信を止め、公開バケット側のACL・配信設定も修正する。既存の保護対象は必要に応じてキーを変更し、旧公開オブジェクト・CDNキャッシュを無効化する。メタデータのCache-Control更新だけで移行完了としない。
7. 別オリジンのフロントエンドや連合配信で必要な取得経路を確認する。公開メディアの連合配信は維持し、保護メディアの連合解除は対象閲覧者・投稿・期限に結び付ける。

### 受入条件

- 匿名、所有者、フォロワー、非フォロワー、解除済み／未解除の利用者について、投稿APIと画像・動画配信の許可結果が一致する。
- 公開→非公開、フォロー解除、解除権限の失効、投稿削除後、新たな取得要求が拒否される。
- 保存した旧URL、ストレージ直URL、CDN、HEAD、Range、偽装ヘッダーによる迂回を拒否する。
- 正当な利用者の画像表示と動画シークが動作する。
- DB・認可ストア停止時に情報を返さない。

### 移行順序

関連付け用のスキーマ追加→既存データの分類と補完→新配信経路の検証→保護オブジェクトの移行→旧経路の遮断とCDN無効化→匿名クライアントで再検証、の順で行う。分類できないデータは公開せず、所有者限定にする。事前に対象件数と移行失敗時の復旧手順を記録する。

## 5. PR 3: Go・依存関係の更新

主な対象: `backend/go.mod`、`go.sum`、`backend/Dockerfile`、CI、開発手順。

監査時点の修正下限は次のとおり。実装時に公式アドバイザリを再確認し、互換性のあるサポート版を選ぶ。「下限以上なら将来の脆弱性もない」という意味ではない。

| 対象 | 監査で示された修正下限 |
|---|---|
| Go | 1.26.6 |
| golang.org/x/text | 0.39.0 |
| golang.org/x/net | 0.55.0 |
| chi/v5 | 5.3.0 |
| AWS SDK service/s3 | 1.97.3 |
| AWS SDK eventstream | 1.7.8 |
| pgx/v5 | 5.9.2 |

Goのローカル・CI・Dockerの版を揃える。AWS SDKは関連モジュールの整合性を保って更新し、S3互換サービスでアップロード、取得、Range、署名URLを確認する。chiはプロキシ設定、pgxはSQLと接続設定も併せて検証する。

受入条件は、`go test ./...`、フロントエンドテストとビルド、Dockerビルド、`govulncheck`、`npm audit`が完了すること。検出が残る場合は適用条件・回避策・担当者・解消期限を記録する。実際に配布するLinuxビルドとコンテナイメージもスキャンし、Windows上の結果だけで完了としない。

## 6. PR 4: ログアウト時のセッション失効

主な対象: `authjwt/jwt.go`、認証ミドルウェア、ログイン・MFA・ログアウト処理、セッション用DB。

### 実装

1. ユーザーのアクセストークンにランダムなセッションIDを持たせ、サーバー側に利用者・有効期限・失効状態を保持する。既存DBを正本とし、保存方式は実装時に負荷を確認する。
2. 必須認証・任意認証の両経路で、署名検証後にセッションを照合する。複数APIインスタンスで失効を共有し、キャッシュによる受理継続を防ぐ。
3. ログアウト時は現在のセッションを失効させ、Cookieを削除する。Cookie認証のログアウトにはCSRF検査を適用する。失効書込み失敗を成功として返さない。
4. 保存先障害時はトークンだけで認証を通さない。再認証導線とエラーメッセージを整える。
5. 旧セッションIDなしトークンの扱いを明示する。切替時に旧ユーザートークンを拒否して再ログインを求める方針とし、互換処理による無期限の迂回を作らない。
6. ユーザーログイン、OAuthトークン、PATを区別する。通常のログアウトでPATを意図せず削除しない。OAuth/PATの失効方針は既存機能と整合させる。

### 受入条件

- ログアウト前にコピーしたトークンをCookie／Bearerで再送しても拒否する。
- 別APIインスタンスでも直ちに拒否する。別端末の有効なセッションは維持する。
- セッションなし旧トークン、期限切れ、失効済み、保存先障害を検証する。
- ログアウトと実行中リクエストの境界を定義する。SSE等の長時間接続は失効通知または定期再検証で終了させる。
- MFAログインと通常ログインの両方でセッションが作成される。

## 7. リリースと再発防止

各PRで監査の再現コードを「攻撃を拒否すべき」回帰テストに変換する。脆弱な挙動が成功することを期待したまま残さない。

CIではGoテスト、フロントエンドのテスト／ビルド、依存関係スキャンを実行する。定期スキャンも設定し、新規の指摘を担当者が追跡できるようにする。例外には期限を設ける。

ステージングで認可の組合せと正常系を検証した後、段階的に本番へ反映する。Pushの拒否数・タイムアウト数・キュー長、メディアの拒否率、認証失効エラー、API遅延を観測する。URL、トークン、DM本文はログに残さない。

不具合時も、SSRF可能な送信経路や無認可のメディア配信へ戻さない。Push停止・保護対象の配信停止で封じ込め、スキーマは追加互換を保って復旧する。旧セッション受理を再開するロールバックも避ける。

## 8. 全体の完了条件

- 監査の4件について、修正コード・回帰テスト・本番設定の確認結果が揃っている。
- 依存関係の検出結果が解消されるか、残存する項目の条件と対応期限が文書化されている。
- 既存メディアと旧セッションの移行が完了し、旧経路による迂回を再検証している。
- 最終コミットと配布イメージを記録し、監査報告書に対応状況を追記している。

工数はメディアの既存件数・共有オブジェクトの有無・連合配信の仕様・本番構成に左右されるため、現段階では日数を固定しない。PR 1と依存関係更新は独立して着手でき、メディアの移行設計が全体日程の主な依存要素になる。
