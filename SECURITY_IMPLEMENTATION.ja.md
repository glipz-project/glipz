# セキュリティ改善の実施記録

実施日: 2026-09-19。対象: `D:\glipz` とローカルDocker環境。本番環境は未定という指定に従い、ローカル開発環境を構築した。

## 実施内容

| 項目 | 実装 |
|---|---|
| Push SSRF | HTTPS・正規サービスのホスト制限、DNS解決後の接続IP検査、リダイレクト禁止、環境HTTPプロキシを不使用。登録時・送信時に検証し、既存の不正購読も削除 |
| Push資源制限 | 15秒の期限、4ワーカー、128件キュー、1分でジョブ失効、1ユーザー10購読、登録頻度制限。過負荷時は通知を破棄 |
| メディア認可 | GET/HEAD/Rangeの配信前に公開範囲、公開日時、停止状態、パスワード・会員解除、DM参加者を確認。未投稿ファイルは所有者のみ。DB障害時は配信拒否 |
| メディア再利用 | 同じオブジェクトを参照する全投稿の権限を満たす必要がある。公開投稿から非公開投稿への迂回を防ぐ |
| キャッシュ・配信経路 | direct設定を配信経路に使用せず、生成URLはAPIプロキシに統一。全メディア応答をprivate,no-storeにし、S3アップロードにも適用 |
| 解除の失効 | パスワード・ロック設定の変更で既存解除を無効化。会員解除は証明の有効期限と15分の短い方に制限 |
| 連合メディア | リモート閲覧者と署名元ホストを一致させ、投稿単位の解除を確認。連合DM添付も署名付きプロキシと参加者検証を使用 |
| ログアウト | JWTにセッションIDを付与し、PostgreSQLで全APIインスタンス共通の有効性を確認。ログアウトで当該セッションを削除。Cookie操作のCSRF検証も実施 |
| SSE | 認証付き接続の資格情報を5秒ごとに再検証し、失効・確認失敗時に切断（照会期限2秒） |
| 依存関係 | Go 1.26.8、Goモジュール、npm依存、Alpine 3.23へ更新。Scalarの外部フォント・AI機能を無効化 |
| 再発防止 | Goの競合検査・govulncheck、フロントのテスト・build・npm audit、イメージのTrivy検査をGitHub Actionsへ追加 |

Pushの`web_push_security` expvarに投入・破棄・期限切れ・拒否・送信失敗を集計する。購読URLや鍵をエラーログへ出さない。既存の保護されたdebug metrics設定を利用する。監視通知先やアラート閾値は本番運用決定時に設定する。

## 検証

- Go全パッケージのテスト成功。Linuxコンテナでの`go test -race ./...`も成功し、データ競合の検出なし。
- フロントエンド7ファイル・22テスト成功、Docker内の本番ビルド成功。
- `scripts/security-smoke.py`成功: メール認証付き登録、アップロード所有権、公開/非公開/ログイン限定/フォロワー限定、GET/HEAD/Range、ヘッダー偽装、パスワード解除と変更後の失効、ログアウト後のBearer再利用拒否、別セッション継続。
- ブラウザー確認: ログインから登録画面への遷移、390px幅の登録画面、APIリファレンスの表示を確認。白画面・エラーオーバーレイなし、ブラウザーエラー0件。専用Browserプラグインを利用できなかったためPlaywrightで確認。
- npmオンライン監査0件。初回監査の0件表示はネットワーク制限下の結果だったため訂正し、オンラインで検出した25件を解消。
- govulncheck: 到達可能な脆弱性0件、インポート対象パッケージの検出0件。モジュール単位では未使用の`golang.org/x/crypto/openpgp`に関する`GO-2026-5932`が残る（修正版なし）。全依存に検出がないという意味ではない。
- 最終アプリイメージのTrivy検査: Critical/High/Medium/Lowはいずれも0件。上記OpenPGPのモジュール単位の指摘がUnknownとして1件残る。

保存結果: `security-govulncheck-after.txt`、`security-npm-audit-after.json`、`security-trivy-summary.json`。

## 起動・移行

[ローカル開発手順](LOCAL_DEVELOPMENT.ja.md)を参照。アプリ、PostgreSQL、Redis、Mailpitの4サービスをループバック限定で起動し、ヘルスチェックを設定した。`.env`にはランダムなローカル用秘密情報を保存し、Git対象外とした。

起動時に`access_sessions`テーブルと投稿オブジェクトキーのGINインデックスを追加する。旧JWTはセッション登録がないため再ログインが必要。PAT・OAuthの独立した資格情報は、このログアウトによる一括失効の対象ではない。

本番導入時はDBをバックアップし、全APIインスタンスを同時に更新する。大規模な投稿テーブルではGINインデックス作成がロックや起動時間に影響するため、事前にステージングで時間を測り、必要なら保守時間帯または別途CONCURRENTLYによる事前作成を行う。

既存の公開バケット、CDN、直接配信パスは本変更だけでは閉じない。本番導入前に公開経路を停止し、CDNキャッシュを無効化し、必要なオブジェクトの再配置を実施する。第三者が保存済みのコピーは回収できない。

## 本番決定後に必要な検証

- 実Pushサービスへの到達と対象ブラウザーでの受信。ローカルではVAPID未設定のため実配信は未検証。
- 2つ以上の実インスタンスを使った連合メディア・DMの相互運用、および実Patreon資格の更新・失効。新しい保護メディア経路には相手側の対応も必要。
- 実S3/CORS設定、既存公開経路の撤去、バックアップ復元、全APIインスタンスの更新手順。
- 通知負荷試験、キュー破棄率・DB負荷・キャッシュ無効化による配信負荷、監視アラートの調整。
- GitHub Actionsの実行結果確認。ワークフローは追加済みだが、リモートへpushしていない。

上記はローカルで完了した修正と分けて管理する。本番の移行完了や実サービスの疎通完了とは扱わない。
